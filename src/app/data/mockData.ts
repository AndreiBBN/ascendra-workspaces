/**
 * @deprecated Import from `../api/workspacesApi` instead.
 * Kept for backward compatibility — all live data comes from `/mock-api/workspaces.json`.
 */
export type {
  VM,
  VMStatus,
  VMUsageHealth,
  VMTemplate,
  FleetMetrics,
  WorkspaceData,
} from '../api/workspacesApi';
